package logic.logic2d

import math.Vec2

class Scene2D(initialId: String = "", initialPosition: Vec2 = Vec2(0.0f, 0.0f), initialAnchorPoint: Vec2 = Vec2(0.0f, 0.0f)): Node2D(initialId, initialPosition, initialAnchorPoint) {
}